curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustup update && rustup install nightly && rustup default nightly
"rustup target add wasm32-unknown-unknown && cargo install wasm-bindgen-cli --version 0.2.88 && sudo apt-get update && sudo apt-get -y install --no-install-recommends xauth libx11-dev libxrandr-dev libxrender-dev",
